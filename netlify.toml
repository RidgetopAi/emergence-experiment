[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[functions]
  node_bundler = "esbuild"

# Note: AIDIS integration is disabled for production deployment
# The application will use static fallback data when AIDIS is not available